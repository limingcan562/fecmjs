/**
 * ---------------------------------
 * This is the dependency plugin of the request module（这是request模块的依赖插件）
 * ---------------------------------
 */
import ajax from "@fdaciuk/ajax";
export default ajax;