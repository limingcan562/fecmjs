/**
 * ---------------------------------
 * Request API（请求相关方法）
 * Be based on @fdaciuk/ajax （基于@fdaciuk/ajax）
 * ---------------------------------
 */

import {Ajax} from '../plugin/ajax/index';

// export const Request = {
//     config: ajax.config,

//     init(myConfig) {
//         ajax.init(myConfig);
//     },

//     base(config = {}) {
//         ajax.base(config = {});
//     },

//     rebuild(config = {}) {
//         ajax.rebuild(config = {});
//     }
// }

export {Ajax};